(()=>{const items=document.querySelectorAll('.reveal-listing');if(!('IntersectionObserver'in window)){items.forEach(x=>x.classList.add('is-visible'))}else{const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('is-visible');observer.unobserve(entry.target)}}),{rootMargin:'0px 0px -7% 0px',threshold:.08});items.forEach(x=>observer.observe(x))}const shell=document.querySelector('.carousel-shell');const toggle=document.querySelector('.carousel-toggle');if(!shell||!toggle)return;toggle.addEventListener('click',()=>{const paused=shell.classList.toggle('is-paused');toggle.setAttribute('aria-pressed',String(paused));toggle.querySelector('.toggle-icon').textContent=paused?'▶':'Ⅱ';toggle.querySelector('.toggle-label').textContent=paused?'Play carousel':'Pause carousel'})})();
