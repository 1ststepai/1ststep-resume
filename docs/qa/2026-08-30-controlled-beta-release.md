# Controlled beta production verification - 2026-08-30 UTC

## Release

- Production deployment: `dpl_QxXHqq91Ssp4EsKQeMbrSurViy2W`
- Deployment URL: `https://1ststep-resume-dxikqrlcg-1ststep.vercel.app`
- Production alias: `https://app.1ststep.ai`
- Ready at: 2026-08-30 02:13:51 UTC
- Verifier: Codex automated checks, for owner review

## Verified

- Remote Vercel build completed and the production deployment reported Ready.
- `npm run test:concierge` passed the complete domain, persistence, isolation, security, worker, document, receipt, and API suite.
- `npm run smoke` passed with zero failures and six allowlisted legacy inline-handler warnings.
- `npm audit --omit=dev` reported zero known production dependency vulnerabilities.
- Ten browser checks passed against the production UI and local rendered employer fixtures: signed-out privacy controls, mobile Saved Info usability, private package review from a simple job card, the few-click guided subscriber launch with a synthetic no-submit run, the signed-out no-charge Job Agent access stop, mobile launch layout without horizontal overflow, safe pause/retry messaging for a slow employer feed, a single consolidated Needs You queue beside simple job cards, ordinary verified-field planning, and secure/consequential Human Action Required routing.
- Shallow readiness returned `ready`; durable storage was reachable; encryption, tenant partitioning, signed receipt ingestion, signed audit, revocable sessions, lifecycle controls, and content-free metrics were configured.
- Production contains encrypted names for every security-gate budget and durable-state dependency, including the explicit `PACKAGE_GLOBAL_DAILY_UNITS` cap; no environment value was printed or copied into this record.
- The synthetic signed-user lifecycle created, restored, and revoked an opaque encrypted session.
- The synthetic application lifecycle created and updated an encrypted session, then verified the signed append-only audit chain.
- At 2026-08-30 03:08:28 UTC, the single cooldown-compliant protected package probe returned HTTP 200 with both `applicationPackageLifecycle` and `applicationPackageArtifactLifecycle` set to `verified`. Durable storage remained reachable and `externalApplicationExecution` remained `false`; the PDFKit Helvetica production-bundle regression is closed.
- A live synthetic U.S.-remote procurement mission against the same deployed discovery code completed in 18.445 seconds across 28 direct-employer sources: 5,586 requisitions scanned, one source error, 5,586 rejected by the mission filter, and zero padded matches. It used no paid job API and kept submissions disabled. The client now allows 30 seconds and explains that broad scans usually take 10-25 seconds.
- A local synthetic package generated a two-page ATS resume and one-page cover letter with zero deterministic QA issues. All three PDF pages were rasterized at 144 DPI and visually inspected: text was legible, single-column, consistently spaced, and free of clipping, overlap, blank pages, or broken glyphs. This does not replace the still-disabled isolated production render gate.
- Unauthenticated tenant operations returned 401, a disallowed origin returned 403, and unsigned internal receipt ingestion returned 401.
- Production returned HSTS with preload, `nosniff`, same-origin referrer policy, denied framing, and a restrictive permissions policy; the documented legacy CSP `unsafe-inline` migration remains open.
- Signed-user, vault, account-data, application-session, and admin-operation endpoints all rejected anonymous access and returned `Cache-Control: no-store` with `Vary: Origin`.
- `externalApplicationExecution` remained `false`; no employer form, personal-data transmission, application submission, or paid service was invoked.
- Local and production SHA-256 hashes matched for all four release-critical static assets:
  - `index.html`: `a8582c2766a93169c7c81c017da58e1e25a68be50bf02386344c1aaaf17e4618`
  - `app.js`: `046a1bb99a47d324f733c1bf7e2d7a2e60aa5d176418cac4c7c18d085f63e66a`
  - `concierge.html`: `dfe8ed4b56f139a3e8a4392489cdd80546d191594629aef8e02fef161b9c0f4e`
  - `concierge.js`: `f1b5f9600a81040cc3d642f9aecc40a381750dc818ec7ea58d88554b33e98602`

## Open gates

- Production does not yet contain `JOB_AGENT_CONSENT_ENFORCEMENT`, `JOB_AGENT_COUNSEL_APPROVED`, the three reviewed policy-version variables, `JOB_AGENT_SCHEDULE_ENABLED`, `JOB_AGENT_SCHEDULE_GLOBAL_DAILY_RUNS`, or `BETA_DATA_ENCRYPTION_KEY_ID`. The local candidate therefore remains fail-closed until counsel approves the customer documents, the owner chooses the controlled-beta daily run cap and non-secret encryption-key ID, and an explicitly approved deployment adds those names.
- The deny-all visual render sandbox is intentionally not configured because activation can incur usage charges and requires owner authorization plus a reviewed snapshot and budget.
- The content-free operator alert destination is intentionally not configured. An approved destination and independently verified receipt are required before relying on alerts.
- Employer browser execution, personal-data transmission, final submission, and receipt capture remain disabled pending isolated-worker implementation, employer/ATS terms review, counsel-approved customer documents, and separate owner authorization.
- A fail-closed employer-browser planning contract and synthetic fixture matrix are implemented locally: exact-host navigation, SSRF rejection, ordinary verified-field references, credential/OTP/CAPTCHA/identity pauses, consequential-question gates, schema-hash checkpoints, and no-submit result validation. The production adapter and provider remain deliberately unconfigured.
- The provider boundary is also implemented behind that disabled flag: it boots only a reviewed snapshot with an exact-employer hostname allowlist, accepts a supplied runner, validates the returned no-submit checkpoint, and always tears down the sandbox. Fake-provider tests prove configuration and crash teardown without creating a real Sandbox.
- Two rendered synthetic employer-form browser tests also pass: ordinary confirmed fields are represented only by fact references, optional demographics remain unanswered, and password, OTP, CAPTCHA, identity, certification, conflict, and clearance fields become Human Action Required without capturing their values.
- A supervised 5-10 user no-submit pilot and real-account lifecycle exercises remain required before expanding access.

This record is a controlled-beta readiness artifact, not authorization to submit applications or advertise unattended auto-apply.

## Local subscriber-shell candidate (not deployed)

- The live asset audit proved that production `concierge.html`, `concierge.js`, and `persistent-concierge.css` match this Vercel-linked checkout byte-for-byte before the local changes. The older `AI-Powered Job Search Platform` checkout is a stale May branch and does not contain the live stylesheet or Vercel project link.
- A local-only subscriber shell now adds My Jobs status tabs, a separate Needs You sheet, canonical receipt-aware badges and counts, a real-settings configuration summary, one-primary-action job cards, action-time confirmation dialogs, restrained toasts, compact mobile navigation, focus trapping, and reduced-motion handling.
- The visible conversation restores only the latest two unique persisted messages, preventing long duplicated or contradictory histories from dominating the subscriber workspace without deleting the underlying local record.
- New tests cover status-to-tab mapping, canonical state consistency, consolidated interruption handling, employer-specific consequence confirmation, and authoritative-receipt-only submission counting.
- The deterministic receipt verifier now rejects cross-host pages, draft/failure language, response-fingerprint mismatches, unauthenticated/spoofed email, unallowlisted or unsigned ATS responses, timestamp inconsistencies, and raw secret evidence. It returns hashes and bounded references, not raw page or email content.
- The signed internal receipt route now feeds raw evidence through that verifier and no longer accepts a caller-authored authoritative-receipt summary. Only the minimized verifier result reaches encrypted durable state; sender/provider allowlists fail closed, and readiness separately reports that live receipt capture is not configured.
- Legacy client-side readiness state no longer offers a real Auto-submit autonomy level, and its old role-transition helper can no longer construct Submitted from typed confirmation fields. Subscriber and admin counts, path-level interview/offer signals, and the acquisition funnel all require employer-side authoritative receipt evidence; absent evidence produces no yield claim.
- New package generation now restores and exactly matches the tenant's finished published direct-employer discovery result. Client-supplied Apply-path booleans, changed requisitions/URLs, and edited employer job text cannot cross the package boundary.
- Local validation before the notification slice: `npm run test:concierge` passed; `npm run smoke` passed with zero failures and six allowlisted warnings; the browser suite passed 16/16 against the local candidate, including admin-only content-free worker health, daily schedule/pause, consent/revocation, secure employer-form fixtures, and 390x844 layout checks.
- A newer local-only consent control plane now requires four affirmative signed-user attestations before discovery, vault writes, package creation/rendering, application-session actions, or durable worker execution. Consent is encrypted and tenant-partitioned, exact policy versions force renewal, revocation pauses active work, and account export/deletion includes the record. Browser checks prove zero discovery runs before consent and an easy revoke-and-pause path. Counsel-approved wording and version values remain an explicit deployment gate.
- A local-only encrypted daily schedule now makes the checked **Keep searching daily** choice real. It stores no plaintext identity or mission in the due index, rechecks current consent before enqueue, enforces one tenant/date run through idempotency, defers at the global daily cap, recovers abandoned claims, and atomically invalidates an active lease on pause/revocation. The connected Vercel team was verified as Pro, so the candidate adds a supported hourly `/api/job-agent-worker` cron that drains at most three due schedules and one recovery run per invocation. No plan or billing setting was changed.
- The admin-only evidence console now shows a content-free background-worker heartbeat and allowlisted counters for invocations, enqueue, replay, consent pause, budget deferral, and scheduler failure. Missing or overdue activity is explicitly `unknown` or `stale`; worker exceptions return HTTP 500 without exposing candidate data, while leased work remains eligible for recovery.
- Production readiness now rejects a configured scheduler whose content-free heartbeat is missing, incomplete, failed, or more than 150 minutes old. The failure is attributed to `background-worker-heartbeat` while durable storage remains truthfully `reachable`, preventing a configured-but-dead cron from passing the launch gate.
- All encrypted persistence surfaces now share a versioned AES-256-GCM keyring: campaign state, applicant vault, consent, schedules, Needs You notification preferences, durable runs/packages, signed-user sessions, application sessions/audit entries, follow-up due records, employer-browser tasks, and resumable browser-session metadata. New envelopes carry the active non-secret key ID; legacy envelopes remain readable through at most three explicitly configured prior keys. A non-destructive synthetic drill verified active write/read and prior-key recovery without printing key material. Production remains unchanged and has not received a key ID or rotated secret.
- A local-only retirement tool now scans only allowlisted encrypted record shapes, verifies decryption in dry mode, emits content-free resumable checkpoints, and requires a separate exact confirmation before atomically rewriting with the active key. Record versions and TTLs are preserved; concurrent-version conflicts are not overwritten. A synthetic Redis capture/delete/restore/decrypt drill is also implemented with guaranteed cleanup, but it has not been run against production. Provider-managed backup/PITR and isolated-database restoration remain unverified external gates.
- A local-only admin audit-head export now emits a separately signed JSON bundle containing an opaque reference, verified ledger head/count, retention policy, and timestamps only. Timeline entries, session identifiers, and candidate field values are excluded; deterministic tests cover redaction, wrong keys, field tampering, digest/signature tampering, and admin authorization. An offline verification command is included. Production has not received `JOB_AGENT_AUDIT_EXPORT_SECRET`, and no retention-locked destination or external archive write was configured, so this remains a release gate rather than claimed archival protection.
- A local-only, one-checkbox Needs You email preference now persists in an encrypted tenant record without storing the recipient address. The server derives the recipient from the signed account and enqueues an encrypted outbox item; the existing heartbeat worker uses short leases, crash recovery, bounded exponential retry, and fixed generic provider copy. Employer, role, requisition, action, question, and candidate details are excluded. Pseudonymous delivery/idempotency records deduplicate retries and are removed on preference/account deletion. Tests cover tenant isolation, exact schema, retry, lease recovery, opt-out, provider-payload redaction, authorization, and a 17/17 browser suite. No email was sent, no provider setting was enabled, and production readiness now fails closed until an approved sender is configured and independently delivery-tested.
- The durable application-session route now accepts a schema-only employer-step checkpoint after current employer-specific transmission approval. It rejects supplied field values and secrets, classifies credentials/challenges/consequential questions into the consolidated Needs You queue, leaves optional demographics unanswered, persists schema hashes and verified fact references without values, enforces a separate account/global worker budget, and uses optimistic concurrency. A tenant-bound encrypted browser-session API and read-only `.example.test` preview now exercise start, resume, expiration, and teardown without a provider call or candidate values. The real remote-stream provider remains deliberately unimplemented, so no employer navigation, personal-data transmission, submission, charge, or provider call can occur. The expanded module/security suite, smoke suite, and 19/19 browser workflows pass locally.
- Provider-cost evidence now covers guided AI, durable role-specific package generation, and the authenticated legacy Claude proxy using aggregate provider-completed requests and provider-reported input/output tokens. The admin-only evidence console displays those totals beside configured global hard ceilings and explicitly labels weighted units as not dollars. No price or cost is inferred; invoice reconciliation remains required before dedicated Job Agent pricing or margin claims.
- The legacy extension autofill path is now locally hardened without changing its Chrome message contract: the browser sends structured candidate/form context, the server owns the model prompt, removes unsafe profile keys and fields before the provider call, and accepts only schema-bound ordinary answers that appear in candidate-provided source text. Extension and server deny categories now align with the Job Agent's Human Action Required policy. Adversarial tests cover protected/consequential fields, fabricated answers, invalid select options, supplied field values, oversized schemas, malformed JSON, and prototype keys. Provider-reported usage is recorded before output validation so rejected replies still remain visible in aggregate cost evidence.
- A local-only shared launch manifest now distinguishes Preview, Signed beta, Package Ready, Assisted application, and Final submission. Signed beta fails closed without an explicitly approved 1-10 user pilot, named support/runbook ownership, counsel-approved policies, cost-limit approval, notification and operator-alert delivery evidence no older than 30 days, and recovery plus provider backup/restore evidence no older than 90 days. The readiness route and admin-only evidence console expose only content-free eligibility and blocker codes. Assisted application and Final submission remain ineligible even when snapshot variables are populated because the live runner and receipt-capture connector do not exist. The production security gate consumes the same manifest to prevent configuration drift.
- A local-only direct-employer truthfulness pass now exact-verifies Greenhouse and Lever requisitions, requires current listed membership for Ashby, allowlists custom Greenhouse Apply hosts per employer, and rejects cross-host or mismatched-requisition URLs. Package creation fails closed when that evidence is absent, future-dated, or more than 24 hours old. Adversarial tests also cover hourly-pay annualization, foreign-only remote roles, phrase-level role mismatch, authorization-neutral scoring, and full-feed supply versus bounded verified-sample labels.
- A read-only public-feed probe at the time of this pass reached all 28 configured employer sources, accepted about 7,811 currently published requisitions with zero invalid Apply paths after exact per-source host review, and found roughly 450 mission-family candidates before the bounded exact-verification sample. These counts are time-sensitive catalog observations, not universal market inventory or outcome evidence. The same probe found only one U.S.-remote procurement-family opening in this catalog, so coverage must be expanded through additional reviewed official sources rather than padded with stale, duplicate, hybrid, misleading, or unsuitable jobs. No candidate data, employer form, paid job API, or submission was used.
- A local-only employer-runner protocol now requires snapshot runner version/digest attestation, resolves only current consented auto-reusable ordinary vault facts, and passes their values through random mode-0600 microVM files rather than command arguments or durable state. Trusted value-free schema inspection and an encrypted tenant-opaque queue reserve the exact schema and fact references atomically with the application session. Pre-start leases recover safely; after execution starts a stale task is never retransmitted, and reconciliation either uses an exact persisted checkpoint or moves the role to Needs You. Browser-task records are included in key rotation, value-free account export, and tenant deletion. The launch manifest requires recent evidence bound to the exact snapshot and runner digest, versioned terms review, and explicit assisted-pilot approval. No sandbox was created and no candidate or employer data was transmitted.
- This candidate has not been deployed. Production remains on deployment `dpl_QxXHqq91Ssp4EsKQeMbrSurViy2W` until explicit owner approval.
- A newer local-only post-submission slice preserves the authoritative receipt while adding tenant-encrypted, signed-audit updates for user-confirmed Interview, Rejected/Closed, and in-app follow-up reminders. Due reminders also enter the single Needs You queue and move the visible run to Waiting for You; no outreach is generated. The complete module/security suite, smoke suite, and 23/23 browser workflows pass locally, including the one-click receipt-to-Interview path. This change is not deployed.
- A newer local-only heartbeat slice atomically pairs scheduled follow-ups with an encrypted tenant-opaque due record, recovers expired claims, and feeds only a fixed generic Needs You alert into the existing opted-in outbox. Employer, role, requisition, application text, and profile data are absent. Current application state is checked both before enqueue and before provider send; completed, moved, closed, deleted, or stale reminders are cancelled. No provider was enabled or contacted, and no employer outreach is generated. This change is not deployed.
- Operator alert readiness evidence is now bound to the exact webhook endpoint, configured receiver contract version, and deterministic fingerprint of the complete fixed-schema event set. Alert payloads and cooldown keys carry that contract identity, so adding submission, receipt, or queue events invalidates stale delivery evidence and cannot be hidden by a prior deduplication claim. Production remains fail-closed until every current content-free event is verified at the receiver and fresh signed evidence is retained.
- Stripe webhook idempotency is now durable across serverless instances: a Redis-backed recoverable lease serializes each HMAC-pseudonymous event, completion is retained for 35 days, failed work is released for retry, and Resend receives a deterministic idempotency key. The old warm-instance `Set` was removed. Signed-beta readiness now requires this control, but production has not received its separate secret and no Stripe test event, CRM mutation, notification, charge, or webhook destination change was performed.
- The local candidate now isolates `/concierge` and `/concierge.html` behind a path-scoped CSP with no inline/eval execution, no style/script attributes, self-only connections/forms, and no frames. Former inline CSS and four runtime style writes were removed; semantic progress elements preserve dynamic status, and the two fixed CDN parsers carry reviewed SHA-384 integrity pins. The production-equivalent local header was exercised in the in-app browser at desktop and 390×844 mobile widths: guided launch, My Jobs, and Needs You rendered and interacted with zero console warnings/errors. The legacy app's broader CSP remains unchanged, and production is not claimed updated before an authorized deployment and live-header check.
