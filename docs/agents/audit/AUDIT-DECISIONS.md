# Audit-derived decisions

Only the Product Owner or Engineering Orchestrator records a decision here after receiving an independent audit handoff. The auditor recommends evidence and severity; it does not own priorities, approve release, or create remediation work.

## 2026-09-12 — Cycle 0 ingestion (Engineering Orchestrator)

- Accepted the immutable external Claude report (SHA-256 936EE4FAFFDAD2914D3161568C563134335CCACA72AA59F597F95F1C680F6344) as a **scoped historical audit baseline**, not a full PR #80 review, release decision or fresh runtime audit. Main d64e174 is the incremental delta anchor for its documented scope; PR #80 b071371 is separately marked PARTIAL.
- Retained AUD-001..016 and cross-referenced the seven existing track IDs. AUD-002 changed to NEEDS REVERIFICATION after a newer associated Preview returned Clerk enabled; AUD-009 and AUD-014 are MITIGATED, not RESOLVED. All 16 remain unresolved until independent evidence meets their resolution contracts. No finding severity was silently downgraded.
- No remediation, Production configuration, deploy, merge, extension publication, roadmap or priority decision was made. The routing table in AUDIT-HANDOFF.md is a proposal only; a bounded task/PR must reference its Audit finding ID. The repository has no separate canonical persistent work queue, so none was created.
- The Product Owner still decides canonical résumé authority, Redis/Postgres beta architecture, Production deploy-source restrictions/linked checkout disposition, extension compatibility policy, partner architecture/public claims, any Clerk credential/domain correction, and autonomous application authority. No risk acceptance or gate waiver is recorded.

Source/evidence: Cycle 0 report plus read-only remote Git/PR, Vercel metadata and app-config, selected PR source, and local worktree checks. Newer Preview's Vercel metadata does not supply a Git SHA; exact artifact parity and signed lifecycle remain unproven. The next independent audit must re-evaluate this ingestion and affected findings.

For each future decision, append (never rewrite history): date, finding ID, decision maker, exact decision and scope, evidence/handoff reference, assigned development owner or reason deferred, required approval, and follow-up verification. A decision to accept risk must name the affected launch gate and owner; it is not an auditor `PASS`.

## 2026-09-13 — RESUME-001 authority contract (Product Owner)

- Scope: AUD-005 and AUD-007. Versioned, verified applicant facts with provenance are factual authority; provenance/verification outrank storage location and timestamp. PARSED, GENERATED, NEWER and LEGACY do not imply VERIFIED. Preserve disagreeing source values, expose material conflicts and require appropriate user review rather than selecting by recency.
- Exactly one explicitly selected, versioned base résumé supplies the document input. Its identity, version and deterministic hash must be pinned into reconciled package input, then into the job-specific artifact and application session. An authoritative employer receipt remains the only Submitted proof. The current Applicant Vault is the account-backed bridge; do not add a competing profile/fact system.
- Preserve `/app/resume` for editing/import/export and backward-compatible `1ststep_resume` review/import. Neither browser storage nor the legacy editor is independent factual authority. Transition: WRAP → MIGRATE → RETIRE REDUNDANT BROWSER AUTHORITY; retirement of the editor is not authorized. No real-user migration in the first implementation.
- AI-generated, rewritten, inferred or parsed material facts require the existing appropriate confirmation/verification mechanism. The proposed Postgres Career Profile is not authoritative before separate migration, hosted RLS, tenant-isolation, recovery, backfill and independent-audit evidence.
- Route bounded implementation to Application Content & Truth with Identity & Data and QA support. Coordinate existing résumé writers, establish one owner/worktree and preserve uncommitted work before edits. Require deterministic conflict/provenance/selection/version-pinning tests. Passing Build evidence may be marked READY FOR INDEPENDENT AUDIT, never AUD-005/AUD-007 RESOLVED. No release-gate waiver or Production authorization follows from this decision.

Source: Product Owner instruction in the Engineering Orchestrator task on 2026-09-13. Existing PR #80 release worktree `release/consolidated-job-agent-v1.6-20260910` at `89cbd017` has uncommitted reconciliation code; the primary product checkout has separate uncommitted vault/package/session changes. Ownership transfer is pending; neither worktree was modified for this decision.

## 2026-09-13 — RESUME-001 release progression gate (Product Owner)

- Treat implementation commit `12a2c36974c52bbf2a6626edd225113ee27987b2` as implementation-complete, **not release-complete**. Preserve the Applicant Vault → explicitly selected version → package path; do not create another authority store or redesign the contract.
- Require an auditor independent of implementation to return PASS or PASS WITH non-blocking follow-up before integration. Reverify the current PR #80 head and all intervening commits, preserve dirty worktrees, test the actual integrated candidate, then create only a clean Preview candidate. No Production deployment follows from this decision.
- Close RESUME-001 only after authenticated hosted Vault persistence, selected-version authority and package identity, conflict behavior, fact-reuse restrictions, truthful failures, and no P0 regression are proven. AUD-005 and AUD-007 retain their separate independent closure gates.
- Advance the next unblocked P0 user-journey workstream in a separate clean worktree and report Reliability / Legacy Resolution separately from Product Advancement. Do not widen into autonomous submission, employer automation, multi-ATS expansion, mobile or unrelated features.

Source: Product Owner instruction in the Engineering Orchestrator task on 2026-09-13. This is a gate decision, not an audit verdict, merge, Preview proof or release authorization.

## 2026-09-14 — First Real User résumé selection, narrative review and paid-access timing (Product Owner)

- **AUD-022 — APPROVED:** Job Agent package generation requires one explicitly selected résumé and version. With no authoritative selection, or when the selected version cannot be securely resolved, generation must stop with an honest non-success state. It must not substitute browser-local, latest, inferred or otherwise arbitrary résumé text. Preserve the legacy editor/import/export as editing surfaces, not alternate package authority. Application Content & Truth owns bounded implementation under the existing App Engineering Orchestrator; verify no-selection and unresolved-selection negative paths, selected-version binding, and visible recovery copy before hosted acceptance and independent re-audit.
- **Pre-generation narrative review — APPROVED FOR CURRENT BETA:** Before final package generation, the user must explicitly review the selected résumé/source facts and material narrative content that cannot yet be deterministically reconciled. Unresolved material narrative conflicts cannot be silently passed into generation. The review must be tied to the particular selected source and fact state so a change requires review again; the Orchestrator chooses implementation details. This is an interim truthful control, not a claim that current deterministic conflict detection works. Deterministic detection may later supersede or reduce manual review only after independent validation. Verify the review and changed-source/fact negative paths in integrated tests, then on an attributable signed Preview; AUD-021 and the narrative gate remain open until evidence is accepted.
- **AUD-019 — DEFERRED:** Paid-access continuity does not block the current clean integration or Preview acceptance. It remains a required owner/engineering decision and verification gate before any deployment beyond Preview **if that release scope depends on paid-access continuity**. This deferral does not authorize a paid beta, waive the finding, or establish that paid users are unaffected; actual impact remains UNKNOWN.

Source: Product Owner's direct “OWNER DECISIONS — RECORD FOR FIRST REAL USER” instruction on 2026-09-14. These decisions settle the named product choices, not the corresponding implementation, independent audit or hosted proof. The App Engineering Orchestrator retains engineering/integration ownership. Do not interrupt its current candidate integration; raise the next owner interruption only for a new product decision from failed integration/re-audit, exact-SHA hosted acceptance readiness, or Production authorization.

## 2026-09-14 — First Real User milestone audit cadence (Product Owner)

- Coordinate continuous deterministic verification during clean integration, then request **one independent audit of the integrated First Real User milestone candidate** when its canonical contract, full-suite results, exact repository/branch/SHA and Git-sourced signed Preview evidence are ready. Do not request a Claude audit after each routine branch, fix or Loop cycle. A deterministic AUD-024 full Vault browser-suite failure at isolated `d2864c8` blocks milestone readiness; it is not a diagnosis that every failing case was introduced by that patch.
- Keep explicit independent review for P0/high-risk auth, tenant, authorization and credential boundaries, and all existing hosted and Production restrictions. The milestone-audit cadence does not close any AUD ID, change auditor authority or grant release approval. Track ENGINEERING → DETERMINISTIC VERIFICATION → MILESTONE READY → AUDIT QUEUED → AUDITING → AUDIT FAILED/REMEDIATING or AUDIT PASSED in the existing ship board; no new ledger or idle polling.

Source: Product Owner routing in the ecosystem Audit Orchestrator task on 2026-09-14 for the App Loop Agent. App Engineering Orchestrator retains candidate construction and integration authority; the independent Auditor owns its actual verdict.
