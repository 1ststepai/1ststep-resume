# 1stStep.ai — Environment Variables
#
# DO NOT commit a real .env file. Set these in Vercel:
#   Project → Settings → Environment Variables
#
# Required:
ANTHROPIC_API_KEY=sk-ant-api03-your-key-here
#
# Get your key at: https://console.anthropic.com → API Keys
# The key only needs "Messages" permission.
#
# Cost estimate per customer order:
#   Essential (no cover letter): ~$0.06–0.10
#   Complete (with cover letter): ~$0.12–0.18
