const $=id=>document.getElementById(id);const nav=$('mobileNav'),toggle=$('navToggle');toggle.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')!=='true';toggle.setAttribute('aria-expanded',String(open));nav.hidden=!open});nav.querySelectorAll('a').forEach(link=>link.addEventListener('click',()=>{toggle.setAttribute('aria-expanded','false');nav.hidden=true}));const referrals=$('referralRange'),months=$('activeMonthsRange');function estimate(){const count=Number(referrals.value),activeMonths=Number(months.value),monthly=count*39*.3;$('referralCount').textContent=count;$('activeMonths').textContent=activeMonths;$('monthlyPotential').textContent=`$${monthly.toLocaleString(undefined,{maximumFractionDigits:0})}`;$('commissionEstimate').textContent=`$${(monthly*activeMonths).toLocaleString(undefined,{maximumFractionDigits:0})}`}$('motionToggle').addEventListener('click',event=>{const paused=document.body.classList.toggle('motion-paused');event.currentTarget.setAttribute('aria-pressed',String(paused));event.currentTarget.textContent=paused?'Resume motion':'Pause motion'});document.querySelectorAll('.reveal').forEach(node=>node.classList.add('is-visible'));referrals.addEventListener('input',estimate);months.addEventListener('input',estimate);estimate();
